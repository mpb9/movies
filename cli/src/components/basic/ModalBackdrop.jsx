export default function ModalBackdrop() {
  // ! add props so i can click this and close the modal
  return (
    <div className="fixed inset-0 z-10 flex items-center justify-center bg-[#040605cc]"></div>
  );
}
